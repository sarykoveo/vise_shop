import React from 'react';
import styles from '../styles/Card.module.scss'

const Card = () => {
    return (
        <div className={styles.main__card}>
            
        </div>
    );
};

export default Card;